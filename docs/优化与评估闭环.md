# 后续可优化点（结合 18 维评估闭环）

> 评估体系已落地：本地裁判读「意图 + Gemini 库存 + 最终 prompt」，按 18 维打分。
> 下列项按优先级排列，可用 `eval.json` 的 `issue_tags` / 低分维度驱动迭代。

## 2026-08-25 针对低分维的管线强化（已落地）

针对实测薄弱维加强了 prompt + 规则校验：

| 维度 | 改动要点 |
| --- | --- |
| 指令理解与遵循 | expand `INTENT SPINE LOCK`；format `INTENT FOLLOW` |
| 参考图/条件控制 | perceive 强化空间/接触/屏上字；identity + asset 用途说明 |
| 角色一致性 | VISUAL IDENTITY LOCK 贯穿 expand/elaborate/format；修复时回灌 inventory |
| 人物与物体交互 | `INTERACTION LOCK` / contact point |
| 空间与三维一致性 | `SPATIAL LOCK`；感知写 left/right、fg/mg/bg |
| 视觉质量 | 可执行光影/材质线索，禁止微物理堆砌 |
| 文字与字幕 | `ON-SCREEN TEXT LOCK` + `extract_locked_onscreen` 硬校验 |
| 音视频同步 | `AV SYNC`；对白时 soundscape=N/A 警告 |

## P0（和测评直接相关）

1. **按低分维反推 prompt 约束**（本轮已对上表 8 维做了一轮）
2. **issue_tags 归类表** 继续用裁判输出驱动下一轮 overlay/阈值微调
3. **裁判与增强模型解耦** 保持独立 `configs/judge.yaml`

## P1 / P2

见原计划：skill 打分稳定性、成片后再评、sglang 20 步参数对齐。
