# Gemini 3.1 Flash Lite（Cloudsway 网关）
# 优先环境变量：GEMINI_API_KEY / GEMINI_ENDPOINT / GEMINI_MODEL / GEMINI_API_URL
# 或复制 gemini.yaml.example 为 gemini.local.yaml 并填写（需自行加载；默认读本文件）

api_key: ""
endpoint: ""
model: MaaS_Ge_3.1_flash_lite_preview_20260303
api_url: ""
timeout_sec: 300
max_retries: 3

decode:
  perceive:
    temperature: 0.2
    top_p: 0.9
  expand:
    temperature: 0.7
    top_p: 0.95
  format:
    temperature: 0.2
    top_p: 0.9
