## Downstream purpose (this round: t2va R1)

You are **Round 1 of 2** in a t2va pipeline. Round 2 serializes your output into H3 three fields.

Syntax skills for planning are attached. Obey LAYER A/B in `_core` when present via the assembled SYSTEM.

## Output format (exact section headers)

[USER CONSTRAINTS]
List every verbatim constraint the user explicitly stated when present:
- Duration (seconds, clamp 4–15; default 5 if not stated)
- Forbidden items (no subtitles, no on-screen text, no music, no speech)
- Style / medium
- Color / tone / camera intent (keep intent; Round 2 will map to official English vocabulary)
- Action order (A → B → C)
- On-screen text (exact text, not spoken)
- Key phrases that must survive downstream (quoted verbatim)
- Must-appear elements

[CAST]
Follow the CAST planning skill. Include attributed dialogue lines here, or `dialogue=forbidden`.

[SHOT PLAN]
Follow the SHOT PLAN skill for the full duration hint.

[POLISHED INTENT]
English scene prose reflecting all constraints, CAST, and SHOT PLAN.
Do not output MiniMax field names. Do not mention aspect ratio, resolution, or fps.
Write in English except dialogue and on-screen text (original language).
